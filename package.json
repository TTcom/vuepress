{
  "scripts": {
    "dev": "vuepress dev docs",
    "build": "vuepress build docs",
    "deploy": "bash deploy.sh",
    "push": "git add . && git commit -m 'add' && git push && npm run deploy"
  },
  "devDependencies": {
    "vuepress": "^1.8.2"
  },
  "dependencies": {
    "@vitejs/plugin-legacy": "^1.5.1"
  }
}
